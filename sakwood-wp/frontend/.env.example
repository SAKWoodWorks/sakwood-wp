# =============================================================================
# SECURITY: Environment Variables Configuration
# =============================================================================
#
# IMPORTANT: Copy this file to .env.local for local development
# NEVER commit .env.local to version control
#
# Prefix Guide:
# - NEXT_PUBLIC_*  = Exposed to client-side (safe to expose)
# - No prefix      = Server-side only (NEVER expose to client)
#
# =============================================================================

# -----------------------------------------------------------------------------
# Client-Side Variables (Safe to expose)
# -----------------------------------------------------------------------------

# WordPress GraphQL API
NEXT_PUBLIC_WORDPRESS_GRAPHQL_URL=http://sak_wp:80/graphql

# WordPress REST API (Custom endpoints - no authentication required)
NEXT_PUBLIC_WORDPRESS_API_URL=http://sak_wp:80/wp-json/sakwood/v1

# WooCommerce REST API (Public endpoints only)
NEXT_PUBLIC_WOOCOMMERCE_API_URL=http://sak_wp:80/wp-json/wc/v3

# App Configuration
NEXT_PUBLIC_APP_NAME=SAK WoodWorks
NEXT_PUBLIC_APP_DESCRIPTION=Premium Construction Materials

# Site URL (for canonical URLs and metadata)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Feature Flags
NEXT_PUBLIC_ENABLE_CART=true

# -----------------------------------------------------------------------------
# Server-Side Only Variables (NEVER expose to client)
# -----------------------------------------------------------------------------
#
# SECURITY WARNING: These values are ONLY available on the server.
# Do NOT add NEXT_PUBLIC_ prefix or they will be exposed in the browser bundle.
#
# -----------------------------------------------------------------------------

# WooCommerce API Credentials
# Required for server-side order status checks
# Get these from: WordPress Admin > WooCommerce > Settings > Advanced > REST API
WOOCOMMERCE_CONSUMER_KEY=ck_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
WOOCOMMERCE_CONSUMER_SECRET=cs_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# JWT Secret for authentication tokens
# Generate a strong random string: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-long

# Session encryption key
# Generate: openssl rand -base64 32
ENCRYPTION_KEY=your-32-character-encryption-key-here

# =============================================================================
# Production Deployment Notes
# =============================================================================
#
# When deploying to production (Vercel, Netlify, etc.):
#
# 1. Set server-side variables (WOOCOMMERCE_*, JWT_*, ENCRYPTION_*)
#    in the deployment platform's environment variables panel
#
# 2. Client-side variables (NEXT_PUBLIC_*) can be in:
#    - .env.production file (committed to git)
#    - Or deployment platform environment variables
#
# 3. NEVER commit actual credentials to git
#    - Add .env.local to .gitignore
#    - Use .env.example for documentation (as this file)
#
# =============================================================================
